<%= form_for(@quote) do |f| %>
    <%= f.text_field :title, placeholder: "Quote title" %>
    <%= f.text_area :quote, rows: 4, placeholder: "Enter quote here" %>
    <%= f.submit "Edit Quote", class: "btn btn-large btn-custom" %>
<% end %>